## 2024-05-22 - Custom Radio Focus Visibility
**Learning:** Custom radio buttons using `sr-only` inputs must have `peer-focus-visible` styles on their visual replacement to be accessible to keyboard users.
**Action:** Always add `peer-focus-visible:ring` styles when hiding the native input.
